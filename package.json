{
  "name": "watch_dir",
  "version": "1.0.0",
  "description": "Watch directories and convert all XML in folders to JSON and output them in user specified directory",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "xml",
    "json"
  ],
  "dependencies": {
    "mkdirp": "^0.5.0",
    "through2": "^0.6.5",
    "watch": "^0.16.0",
    "xml2js": "^0.4.8"
  },
  "author": "Brian Klein",
  "license": "ISC"
}
